# TODO: 게임 감독 및 게임 오버

## 목적
- 스코어 및 시간 연산, HP가 0이 될 때의 게임 종료 및 결과 출력 흐름 제어

## 세부 태스크
1. **생존 시간 및 스코어 누적**
   - `GameDirector.cs`를 구현하여 `Time.deltaTime`으로 생존 시간을 실시간 계산.
   - 오크 처치 카운트를 기반으로 스코어 누적.
   - TextMeshPro 컴포넌트를 활용해 화면 상단에 생존 시간 및 점수 출력.
2. **게임 오버 판정**
   - 플레이어 체력이 0 이하가 되면 게임 루프 정지 (`Time.timeScale = 0` 등).
   - 화면 중앙에 최종 기록 결과창 UI 활성화.
